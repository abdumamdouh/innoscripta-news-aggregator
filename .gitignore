# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage
.playwright-mcp
playwright-report
test-results

# Secrets. .env.example is the only env file that belongs in the repo.
.env
.env.*
!.env.example

# Local tooling notes — kept on this machine, not part of the project.
GOAL.md
.claude/
.implement-feature-loop/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel's local link + the OIDC token it writes into .env.local. The env rules above
# already cover that file; a bare `.env*` here would sit after the !.env.example negation
# and quietly re-ignore the one env file that belongs in the repo.
.vercel
