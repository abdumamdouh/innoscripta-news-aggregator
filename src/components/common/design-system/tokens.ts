/** Shared class fragments every primitive needs. Ownerless on purpose: no primitive
 *  should have to import a sibling just to focus correctly. */
export const appFocusRing =
  'focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent-600'
