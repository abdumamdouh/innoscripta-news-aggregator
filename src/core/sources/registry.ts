import type { NewsSource } from '@/core/sources/types'

/**
 * The only file that changes when a source is added: one import, one entry.
 * Adapters land with backlog item 3.
 */
export const SOURCES: NewsSource[] = []
