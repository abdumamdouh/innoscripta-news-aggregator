/**
 * Preferences own no route — they are a modal opened from the header — so the feature's
 * public surface is its trigger, plus the read side item 8's feed consumes.
 */
export { PreferencesButton } from '@/features/Preferences/components/PreferencesButton'
export { usePreferences } from '@/features/Preferences/hooks/usePreferences'
