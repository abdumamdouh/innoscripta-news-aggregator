{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": ".claude/hooks/guard-generated.sh"
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -qE '\\.(tsx|ts)$' && printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"[RULES] App* wrappers over Radix, never Radix directly in features | theme tokens, no hex | no provider field names outside src/core/sources/adapters | no VITE_* key in client code | i18n flat keys in BOTH en+ar | yup in *.schema.ts via create*Schema(t) | @/ imports, no ../../.. | no any\"}}' || true",
            "statusMessage": "Checking project rules..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": ".claude/hooks/format-on-edit.sh"
          }
        ]
      }
    ]
  }
}
